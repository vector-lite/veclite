# Java 重构与编码约束

本项目使用 Java 17。后续重构和新功能开发必须在保持既有 API 与行为兼容的前提下遵循以下规则。

## 设计原则

- 严格遵循 SOLID：类和方法保持单一职责，依赖通过接口注入，扩展优先新增实现而不是修改稳定逻辑。
- 当同一职责出现 4 个或更多条件分支时，使用“策略接口 + 工厂”封装变化点；工厂只负责选择策略，策略只负责业务动作。
- 方法只完成一个清晰动作；复杂流程拆分为命名明确的私有方法。
- 优先使用 Java 17 的 `record` 表达不可变数据，使用 `switch` 表达式处理枚举分派。

## 数据与数值

- 所有金额、费率、折扣和计费结果必须使用 `BigDecimal`，禁止使用 `double` 或 `float` 表示金额。
- 小数计算必须显式指定舍入模式与精度；禁止依赖隐式二进制浮点转换。
- 消除魔法值：使用枚举、命名常量或集中配置表达状态、阈值、默认值和协议常量。

## 文档与兼容性

- 每个 `public` 类、构造器和方法必须有完整 JavaDoc，说明用途、参数、返回值、异常及兼容性注意事项。
- 关键业务节点（分支选择、持久化边界、外部调用和数据转换）必须有 JavaDoc 或简洁注释。
- 重构前后保持公开方法签名、异常语义、序列化格式和默认行为不变；新增行为必须配套回归测试。

## 验证

- 先运行受影响的测试类，再运行 `./gradlew test`。
- 测试应覆盖正常输入、空值、非法输入和边界条件；随机数据使用固定种子。
